# 预约挂号流程图

## 1. 预约成功流程图

```mermaid
flowchart TD
    Start([患者开始预约]) --> Step1[1. 选择医生和日期]
    Step1 --> Step2[2. 前端请求获取时间段<br/>GET /order/findOrderTime?arId=排班ID]
    Step2 --> Step3{3. 查询排班信息}
    Step3 -->|排班不存在| Fail1[❌ 失败: 排班信息不存在]
    Step3 -->|排班存在| Step4[4. 计算每个时间段剩余号数<br/>总号数10 - 已预约数]
    Step4 --> Step5[5. 返回时间段列表给前端<br/>显示可预约时间段]
    Step5 --> Step6[6. 患者选择时间段]
    Step6 --> Step7[7. 前端提交预约请求<br/>POST /patient/addOrder<br/>参数: order, arId]
    Step7 --> Step8[8. 后端接收请求]
    Step8 --> Check1{9. 验证时间段格式}
    Check1 -->|格式错误| Fail2[❌ 失败: 时间段格式错误]
    Check1 -->|格式正确| Step9[10. 提取日期和时间段]
    Step9 --> Check2{11. 检查患者当天是否已有预约}
    Check2 -->|已有预约| Fail3[❌ 失败: 您今天已有预约<br/>一天只能预约一次]
    Check2 -->|无预约| Check3{12. 判断是否为当天预约?}
    Check3 -->|是当天| Check4{13. 检查时间段是否在当前时间之后?}
    Check4 -->|时间段已过| Fail4[❌ 失败: 当天只能预约<br/>当前时间之后的时间段]
    Check4 -->|时间段未过| Check5{14. 检查时间段剩余号数}
    Check3 -->|非当天| Check5
    Check5 -->|剩余号数 <= 0| Fail5[❌ 失败: 该时间段号数已满<br/>请选择其他时间段]
    Check5 -->|剩余号数 > 0| Check6{15. 检查患者在该时间段<br/>是否已有未诊断挂号单?}
    Check6 -->|已有| Fail6[❌ 失败: 您在该时间段已有预约]
    Check6 -->|无| Step10[16. 生成挂号单号<br/>随机生成 1000-9999]
    Step10 --> Step11[17. 设置初始状态<br/>oState = 0 未完成<br/>oPriceState = 0 未缴费<br/>oStart = 预约时间段]
    Step11 --> Step12[18. 保存挂号单到数据库]
    Step12 --> Check7{19. 保存是否成功?}
    Check7 -->|保存失败| Fail7[❌ 失败: 挂号失败<br/>数据库保存异常]
    Check7 -->|保存成功| Success[✅ 预约成功]
    Success --> Step13[20. 返回成功响应<br/>R.ok 挂号成功]
    Step13 --> Step14[21. 前端显示成功提示]
    Step14 --> Step15[22. 更新页面数据<br/>刷新预约列表]
    Step15 --> End([预约完成])
    
    Fail1 --> End
    Fail2 --> End
    Fail3 --> End
    Fail4 --> End
    Fail5 --> End
    Fail6 --> End
    Fail7 --> End
    
    style Success fill:#90EE90,stroke:#006400,stroke-width:3px
    style Step13 fill:#90EE90
    style Step14 fill:#90EE90
    style Step15 fill:#90EE90
    style End fill:#87CEEB,stroke:#4682B4,stroke-width:2px
    style Fail1 fill:#FFB6C1,stroke:#DC143C,stroke-width:2px
    style Fail2 fill:#FFB6C1,stroke:#DC143C,stroke-width:2px
    style Fail3 fill:#FFB6C1,stroke:#DC143C,stroke-width:2px
    style Fail4 fill:#FFB6C1,stroke:#DC143C,stroke-width:2px
    style Fail5 fill:#FFB6C1,stroke:#DC143C,stroke-width:2px
    style Fail6 fill:#FFB6C1,stroke:#DC143C,stroke-width:2px
    style Fail7 fill:#FFB6C1,stroke:#DC143C,stroke-width:2px
```

### 预约成功流程说明

**成功路径的关键步骤：**
1. ✅ 时间段格式正确
2. ✅ 患者当天无其他预约
3. ✅ 当天预约时，时间段在当前时间之后
4. ✅ 时间段剩余号数 > 0
5. ✅ 患者在该时间段无未诊断挂号单
6. ✅ 数据库保存成功

**预约成功后的状态：**
- 挂号单号 (oId): 随机生成 1000-9999
- 挂号状态 (oState): 0 (未完成/待就诊)
- 缴费状态 (oPriceState): 0 (未缴费)
- 开始时间 (oStart): 预约的时间段，格式：`yyyy-MM-dd HH:mm-HH:mm`
- 患者ID (pId): 当前登录患者ID
- 医生ID (dId): 选择的医生ID

---

## 2. 预约失败流程图

```mermaid
flowchart TD
    Start([患者提交预约请求]) --> Step1[后端接收请求<br/>POST /patient/addOrder]
    Step1 --> Step2[开始验证流程]
    Step2 --> Check1{验证1: 时间段格式验证}
    Check1 -->|格式错误<br/>oStart长度或格式不正确| Fail1[❌ 失败原因1<br/>时间段格式错误<br/><br/>错误提示: 时间段格式错误]
    Check1 -->|格式正确| Check2{验证2: 患者当天预约限制}
    Check2 -->|已有预约<br/>countPatientOrdersByDate > 0| Fail2[❌ 失败原因2<br/>患者当天已有预约<br/><br/>错误提示: 您今天已有预约<br/>一天只能预约一次]
    Check2 -->|无预约| Check3{验证3: 判断是否为当天预约?}
    Check3 -->|是当天| Check4{验证4: 时间段时间限制}
    Check4 -->|时间段已过当前时间<br/>startTime <= currentTime| Fail3[❌ 失败原因3<br/>当天时间段已过<br/><br/>错误提示: 当天只能预约<br/>当前时间之后的时间段]
    Check4 -->|时间段未过| Check5{验证5: 时间段剩余号数}
    Check3 -->|非当天| Check5
    Check5 -->|剩余号数 <= 0<br/>totalCount - bookedCount <= 0| Fail4[❌ 失败原因4<br/>时间段号数已满<br/><br/>错误提示: 该时间段号数已满<br/>请选择其他时间段]
    Check5 -->|剩余号数 > 0| Check6{验证6: 重复预约检查}
    Check6 -->|已有未诊断挂号单<br/>existOrders不为空| Fail5[❌ 失败原因5<br/>患者在该时间段已有预约<br/><br/>错误提示: 您在该时间段已有预约]
    Check6 -->|无重复预约| Step3[生成挂号单信息]
    Step3 --> Step4[保存挂号单到数据库]
    Step4 --> Check7{验证7: 数据库保存}
    Check7 -->|保存失败<br/>数据库异常或约束冲突| Fail6[❌ 失败原因6<br/>数据库保存失败<br/><br/>错误提示: 挂号失败<br/>当前时间段存在还未诊断的挂号单]
    Check7 -->|保存成功| Success[✅ 预约成功]
    
    Fail1 --> End1([预约失败])
    Fail2 --> End1
    Fail3 --> End1
    Fail4 --> End1
    Fail5 --> End1
    Fail6 --> End1
    Success --> End2([预约成功])
    
    style Success fill:#90EE90,stroke:#006400,stroke-width:3px
    style End2 fill:#87CEEB,stroke:#4682B4,stroke-width:2px
    style Fail1 fill:#FFB6C1,stroke:#DC143C,stroke-width:2px
    style Fail2 fill:#FFB6C1,stroke:#DC143C,stroke-width:2px
    style Fail3 fill:#FFB6C1,stroke:#DC143C,stroke-width:2px
    style Fail4 fill:#FFB6C1,stroke:#DC143C,stroke-width:2px
    style Fail5 fill:#FFB6C1,stroke:#DC143C,stroke-width:2px
    style Fail6 fill:#FFB6C1,stroke:#DC143C,stroke-width:2px
    style End1 fill:#FFA07A,stroke:#FF6347,stroke-width:2px
```

### 预约失败原因详解

| 失败原因 | 验证检查点 | 验证逻辑 | 错误提示信息 |
|---------|-----------|---------|------------|
| **失败1** | 时间段格式验证 | `oStart` 长度或格式不正确 | 时间段格式错误 |
| **失败2** | 患者当天预约限制 | `countPatientOrdersByDate(pId, date) > 0` | 您今天已有预约，一天只能预约一次 |
| **失败3** | 当天时间段限制 | 当天预约时，`startTime <= currentTime` | 当天只能预约当前时间之后的时间段 |
| **失败4** | 时间段剩余号数 | `totalCount(10) - bookedCount <= 0` | 该时间段号数已满，请选择其他时间段 |
| **失败5** | 重复预约检查 | 患者在该时间段已有 `oState=0` 的挂号单 | 您在该时间段已有预约 |
| **失败6** | 数据库保存 | 保存时发生异常或约束冲突 | 挂号失败，当前时间段存在还未诊断的挂号单 |

### 失败处理机制

所有失败情况都会：
1. 抛出 `RuntimeException` 异常，包含具体的错误信息
2. Controller 层捕获异常并返回 `R.error(错误信息)`
3. 前端接收错误响应并显示错误提示
4. 用户可以根据错误提示调整预约信息后重试

### 关键代码位置

**核心验证方法：**
- `OrderServiceImpl.addOrder(Orders order, String arId)` - 主入口方法
- `OrderServiceImpl.hasPatientOrderToday(Integer pId, String date)` - 检查当天预约
- `OrderServiceImpl.canBookTimeSlot(String timeSlot)` - 检查时间段是否可预约
- `OrderServiceImpl.getRemainingCountForTimeSlot(Integer dId, String date, String timeSlot)` - 获取剩余号数

**数据库查询方法：**
- `OrderMapper.countOrdersByTimeSlot(dId, date, timeSlot)` - 统计时间段预约数
- `OrderMapper.countPatientOrdersByDate(pId, date)` - 统计患者当天预约数

---

## 3. 完整预约流程（包含成功和失败分支）

```mermaid
flowchart TD
    Start([开始预约]) --> Step1[1. 获取可预约时间段]
    Step1 --> Step2[2. 前端显示时间段选择界面]
    Step2 --> Step3[3. 患者选择时间段]
    Step3 --> Step4[4. 提交预约请求]
    
    Step4 --> Check1{检查1: 时间段格式}
    Check1 -->|格式错误| Fail1[❌ 失败: 时间段格式错误]
    Check1 -->|格式正确| Check2{检查2: 患者当天是否已有预约}
    
    Check2 -->|已有预约| Fail2[❌ 失败: 您今天已有预约<br/>一天只能预约一次]
    Check2 -->|无预约| Check3{检查3: 是否为当天预约?}
    
    Check3 -->|是当天| Check4{检查4: 时间段是否在当前时间之后?}
    Check4 -->|时间段已过| Fail3[❌ 失败: 当天只能预约<br/>当前时间之后的时间段]
    Check4 -->|时间段未过| Check5{检查5: 时间段剩余号数}
    Check3 -->|非当天| Check5
    
    Check5 -->|剩余号数 <= 0| Fail4[❌ 失败: 该时间段号数已满<br/>请选择其他时间段]
    Check5 -->|剩余号数 > 0| Check6{检查6: 患者在该时间段<br/>是否已有未诊断挂号单?}
    
    Check6 -->|已有| Fail5[❌ 失败: 您在该时间段已有预约]
    Check6 -->|无| Step5[5. 生成挂号单号<br/>随机1000-9999]
    
    Step5 --> Step6[6. 设置初始状态<br/>oState=0 未完成<br/>oPriceState=0 未缴费]
    Step6 --> Step7[7. 保存挂号单到数据库]
    
    Step7 --> Check7{检查7: 保存是否成功?}
    Check7 -->|保存失败| Fail6[❌ 失败: 挂号失败<br/>当前时间段存在还未诊断的挂号单]
    Check7 -->|保存成功| Success[✅ 成功: 挂号成功]
    
    Success --> End1([预约完成])
    Fail1 --> End2([预约失败])
    Fail2 --> End2
    Fail3 --> End2
    Fail4 --> End2
    Fail5 --> End2
    Fail6 --> End2
    
    style Success fill:#90EE90
    style Fail1 fill:#FFB6C1
    style Fail2 fill:#FFB6C1
    style Fail3 fill:#FFB6C1
    style Fail4 fill:#FFB6C1
    style Fail5 fill:#FFB6C1
    style Fail6 fill:#FFB6C1
    style End1 fill:#87CEEB
    style End2 fill:#FFA07A
```

## 4. 预约验证检查点详解

### 验证检查点列表

| 检查点 | 验证内容 | 失败原因 | 错误提示 |
|--------|---------|---------|---------|
| 检查1 | 时间段格式验证 | 时间段格式不正确 | 时间段格式错误 |
| 检查2 | 患者当天预约限制 | 患者当天已有预约 | 您今天已有预约，一天只能预约一次 |
| 检查3 | 判断是否为当天预约 | - | - |
| 检查4 | 当天时间段限制 | 时间段已过当前时间 | 当天只能预约当前时间之后的时间段 |
| 检查5 | 时间段剩余号数 | 剩余号数 <= 0 | 该时间段号数已满，请选择其他时间段 |
| 检查6 | 重复预约检查 | 患者在该时间段已有未诊断挂号单 | 您在该时间段已有预约 |
| 检查7 | 数据库保存 | 保存失败 | 挂号失败，当前时间段存在还未诊断的挂号单 |

## 5. 预约成功后的状态

### 挂号单初始状态
- **挂号单号 (oId)**: 随机生成 1000-9999
- **挂号状态 (oState)**: 0 (未完成/待就诊)
- **缴费状态 (oPriceState)**: 0 (未缴费)
- **开始时间 (oStart)**: 预约的时间段，格式：`yyyy-MM-dd HH:mm-HH:mm`
- **结束时间 (oEnd)**: null (就诊完成后才设置)
- **患者ID (pId)**: 当前登录患者ID
- **医生ID (dId)**: 选择的医生ID

### 后续流程
1. **待就诊阶段**: 患者等待就诊时间
2. **就诊阶段**: 医生进行诊断，更新挂号单信息
3. **完成就诊**: 设置 `oState = 1`，`oEnd = 当前时间`
4. **缴费阶段**: 患者缴费，设置 `oPriceState = 1`

## 6. 关键代码逻辑位置

### 核心方法
- **获取时间段**: `OrderServiceImpl.findOrderTime(String arId)`
- **添加挂号**: `OrderServiceImpl.addOrder(Orders order, String arId)`
- **计算剩余号数**: `OrderServiceImpl.calculateTimeSlots(Integer dId, String date)`
- **检查当天预约**: `OrderServiceImpl.hasPatientOrderToday(Integer pId, String date)`
- **检查时间段**: `OrderServiceImpl.canBookTimeSlot(String timeSlot)`
- **获取剩余号数**: `OrderServiceImpl.getRemainingCountForTimeSlot(Integer dId, String date, String timeSlot)`

### 数据库查询
- **统计时间段预约数**: `OrderMapper.countOrdersByTimeSlot(dId, date, timeSlot)`
- **统计患者当天预约数**: `OrderMapper.countPatientOrdersByDate(pId, date)`

